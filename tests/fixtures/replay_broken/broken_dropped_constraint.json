{
  "name": "broken_dropped_constraint",
  "description": "DELIBERATELY BROKEN: turn 2 re-requests the excluded genre, so minimal precedence suspends it and a comedy IS recommended - yet the script (wrongly) asserts the exclusion still holds. The suite must DETECT this as a failed invariant, proving it has teeth.",
  "turns": [
    {
      "user": "action movies but no comedy",
      "invariants": [
        {"kind": "constraint_holds", "exclude_genres": ["Comedy"]}
      ]
    },
    {
      "user": "actually show me a comedy",
      "invariants": [
        {"kind": "constraint_holds", "exclude_genres": ["Comedy"]}
      ]
    }
  ]
}
